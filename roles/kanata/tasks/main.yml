---
- name: "DESK_KANATA_MAIN - Install kanata binary and uinput setup"
  ansible.builtin.include_tasks: install.yml

- name: "DESK_KANATA_MAIN - Configure per-user kanata config and systemd service"
  ansible.builtin.include_tasks: configure.yml
