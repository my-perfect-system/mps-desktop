---
- name: QTILE - Compute per-user facts
  ansible.builtin.include_tasks: facts.yml
- name: "DESK_QTILE_MAIN - Install qtile (packages, repo, venv, xsession, system scripts)"
  ansible.builtin.include_tasks: install.yml

- name: "DESK_QTILE_MAIN - Configure per-user qtile config + scripts"
  ansible.builtin.include_tasks: configure.yml
